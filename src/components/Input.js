import React from 'react';

function Input(props) {
   return(
       <input
       id={props.id}
       className={props.className}
       type={props.type}
       >
       title.props
       </input>
   )
}

export default Input;